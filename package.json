{
  "name": "@feedjar/sdk-web",
  "version": "0.0.1",
  "private": false,
  "description": "FeedJar browser SDK — configure, submit, and list feedback.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
