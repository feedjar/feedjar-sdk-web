{
  "name": "@feedjar/sdk-web",
  "version": "0.1.0",
  "private": true,
  "description": "FeedJar browser SDK — configure, submit, and list feedback (parity with iOS/Android kits).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
